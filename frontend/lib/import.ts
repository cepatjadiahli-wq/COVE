export function parseCsv(text:string):string[][]{const rows:string[][]=[];let row:string[]=[];let field='';let quoted=false;for(let i=0;i<text.length;i++){const c=text[i];if(c==='"'){if(quoted&&text[i+1]==='"'){field+='"';i++;}else quoted=!quoted;}else if(c===','&&!quoted){row.push(field);field='';}else if((c==='\n'||c==='\r')&&!quoted){if(c==='\r'&&text[i+1]==='\n')i++;row.push(field);if(row.some(v=>v.trim()))rows.push(row);row=[];field='';}else field+=c;}if(quoted)throw Error('Tanda kutip CSV tidak lengkap.');row.push(field);if(row.some(v=>v.trim()))rows.push(row);if(rows[0])rows[0][0]=rows[0][0].replace(/^\uFEFF/,'');return rows;}
export function validateImport(rows:string[][],nameIndex:number,amountIndex:number){return rows.map((row,i)=>{const name=(row[nameIndex]??'').trim();const raw=(row[amountIndex]??'').trim();const amount=Number(raw);return {row:i+2,name,amount,error:!name?'Nama pekerjaan wajib diisi.':raw===''||!Number.isFinite(amount)||amount<0?'Nilai harus angka positif atau nol, tanpa pemisah ribuan.':''};});}
